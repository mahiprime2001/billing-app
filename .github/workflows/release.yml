name: "Tauri Windows Auto Release"

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  prepare-environment:
    name: 🧮 Prepare Version & Environment
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.calculate.outputs.version }}
    steps:
      - name: 🛒 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⚙️ Install dependencies
        run: sudo apt-get install -y jq

      - id: calculate
        shell: bash
        run: |
          set -euo pipefail
          TAURI_FILE="src-tauri/tauri.conf.json"
          git fetch --tags --force
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          RANGE=${PREV_TAG:+$PREV_TAG..HEAD}
          if git log $RANGE --pretty=%B | grep -q "BREAKING CHANGE"; then TYPE="major"
          elif git log $RANGE --pretty=%B | grep -q "^feat:"; then TYPE="minor"
          elif git log $RANGE --pretty=%B | grep -q "^fix:"; then TYPE="patch"
          else TYPE="patch"; fi
          OLD_VERSION=$(jq -r '.version' "$TAURI_FILE")
          if [ -z "$OLD_VERSION" ] || [ "$OLD_VERSION" = "null" ]; then
            echo "❌ Version not found in $TAURI_FILE"
            exit 1
          fi
          IFS='.' read -r MAJOR MINOR PATCH <<< "$OLD_VERSION"
          case "$TYPE" in
            major) MAJOR=$((MAJOR+1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR+1)); PATCH=0 ;;
            patch) PATCH=$((PATCH+1)) ;;
          esac
          NEW_VERSION="$MAJOR.$MINOR.$PATCH"
          while git rev-parse "v${NEW_VERSION}" >/dev/null 2>&1; do
            PATCH=$((PATCH+1))
            NEW_VERSION="$MAJOR.$MINOR.$PATCH"
          done
          echo "version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: 📝 Update version in Tauri config
        run: |
          jq ".version = \"${{ steps.calculate.outputs.version }}\"" src-tauri/tauri.conf.json > tmp.$$.json && mv tmp.$$.json src-tauri/tauri.conf.json

      - name: 💾 Commit updated version to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src-tauri/tauri.conf.json
          git commit -m "chore: bump version to v${{ steps.calculate.outputs.version }}" || echo "No version changes to commit"
          git push origin HEAD:main

      - name: 🏷️ Create and push tag
        run: |
          git tag "v${{ steps.calculate.outputs.version }}" || echo "Tag exists"
          git push origin "v${{ steps.calculate.outputs.version }}" || echo "⚠️ Tag already exists, continuing"

  build-and-release-windows:
    name: 🪟 Build & Release Windows
    needs: prepare-environment
    runs-on: windows-latest
    steps:
      - name: 🛒 Checkout code (latest main)
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: ⚙️ Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: 🧰 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗️ Build Tauri App (Windows)
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npx @tauri-apps/cli build

      - name: 📦 Prepare Updater Assets (Zip & Sign Bundles)
        env:
          TAURI_UPDATER_PRIVATE_KEY: ${{ secrets.TAURI_UPDATER_PRIVATE_KEY }}
          TAURI_UPDATER_KEY_PASSWORD: ${{ secrets.TAURI_UPDATER_KEY_PASSWORD }}  # Plain passphrase
        shell: bash
        run: |
          set -euo pipefail
          BUNDLE_DIR="src-tauri/target/release/bundle"
          VERSION="${{ needs.prepare_environment.outputs.version }}"
          REPO="${{ github.repository }}"
          TAG="v$VERSION"

          # Write raw PEM key from secret to temp file, clean whitespace/empty lines (single robust command)
          PRIVATE_KEY_FILE="/tmp/updater.key"
          # Assign secret to var, then clean: trim leading/trailing whitespace/empty lines, normalize
          KEY_CONTENT=$(echo "$TAURI_UPDATER_PRIVATE_KEY" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | sed '/^$/d' | sed '1s/^[[:space:]]*//' | sed '$s/[[:space:]]*$//')
          echo "$KEY_CONTENT" > "$PRIVATE_KEY_FILE"

          # Validate PEM structure (starts/ends exactly with headers)
          if ! head -n 1 "$PRIVATE_KEY_FILE" | grep -q "^-----BEGIN PRIVATE KEY-----$"; then
            echo "❌ Temp key file malformed (header mismatch)"
            echo "First line debug (masked):"
            head -n 1 "$PRIVATE_KEY_FILE" | sed 's/./*/g'
            exit 1
          fi
          if ! tail -n 1 "$PRIVATE_KEY_FILE" | grep -q "^-----END PRIVATE KEY-----$"; then
            echo "❌ Temp key file malformed (footer mismatch)"
            exit 1
          fi
          echo "✅ Temp key file cleaned and validated (PEM ready)"

          # Find bundles dynamically
          MSI_FILE=$(find "$BUNDLE_DIR/msi" -name "*.msi" | head -1)
          EXE_FILE=$(find "$BUNDLE_DIR/nsis" -name "*setup.exe" | head -1)

          if [ -z "$MSI_FILE" ] || [ -z "$EXE_FILE" ]; then
            echo "❌ No bundle files found!"
            exit 1
          fi

          echo "Found MSI: $MSI_FILE"
          echo "Found EXE: $EXE_FILE"

          # Dynamic zip paths
          MSI_ZIP="$BUNDLE_DIR/msi/$(basename "$MSI_FILE").zip"
          EXE_ZIP="$BUNDLE_DIR/nsis/$(basename "$EXE_FILE").zip"

          # Zip bundles
          7z a "$MSI_ZIP" "$MSI_FILE"
          7z a "$EXE_ZIP" "$EXE_FILE"

          echo "Created MSI ZIP: $MSI_ZIP"
          echo "Created EXE ZIP: $EXE_ZIP"

          # Sign using cleaned temp PEM file + password
          MSI_SIGNATURE=$(npx @tauri-apps/cli signer sign --private-key-path "$PRIVATE_KEY_FILE" --password "$TAURI_UPDATER_KEY_PASSWORD" "$MSI_ZIP")
          EXE_SIGNATURE=$(npx @tauri-apps/cli signer sign --private-key-path "$PRIVATE_KEY_FILE" --password "$TAURI_UPDATER_KEY_PASSWORD" "$EXE_ZIP")

          # Clean up
          rm -f "$PRIVATE_KEY_FILE"

          # Generate update.json
          PUB_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          cat > "$BUNDLE_DIR/update.json" << EOF
          {
            "version": "$VERSION",
            "notes": "🚀 Added auto-update support and fixed minor bugs.",
            "pub_date": "$PUB_DATE",
            "platforms": {
              "windows-x86_64": {
                "signature": "$MSI_SIGNATURE",
                "url": "https://github.com/$REPO/releases/download/$TAG/$(basename "$MSI_ZIP")"
              },
              "windows-x86_64-nsis": {
                "signature": "$EXE_SIGNATURE",
                "url": "https://github.com/$REPO/releases/download/$TAG/$(basename "$EXE_ZIP")"
              }
            }
          }
          EOF

          # Validate JSON
          jq . "$BUNDLE_DIR/update.json" > /dev/null || { echo "❌ Invalid JSON"; exit 1; }

          echo "Generated update.json:"
          cat "$BUNDLE_DIR/update.json"

          # Assets
          ASSETS=("$MSI_ZIP" "$EXE_ZIP" "$BUNDLE_DIR/update.json")
          echo "ASSETS<<EOF" >> $GITHUB_ENV
          for asset in "${ASSETS[@]}"; do echo "$asset" >> $GITHUB_ENV; done
          echo "EOF" >> $GITHUB_ENV

      - name: 🚀 Create or Update GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="v${{ needs.prepare_environment.outputs.version }}"
          ASSETS=$(cat << 'EOF' | tr '\n' ' '
          ${{ env.ASSETS }}
          EOF
          )

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "⚠️ Release $TAG exists, updating..."
            gh release upload "$TAG" $ASSETS --clobber || echo "ℹ️ No assets to replace."
          else
            echo "🆕 Creating release $TAG"
            gh release create "$TAG" $ASSETS \
              --title "Siri Admin App $TAG" \
              --notes "Automated Tauri Windows release with updater support for version $TAG"
          fi

          gh release upload "$TAG" "src-tauri/target/release/bundle/update.json" latest.json --clobber

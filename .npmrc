# Ensure package-lock.json is generated with exact versions
package-lock=true

# Use the latest npm for better performance and features
export NPM_CONFIG_UPDATE_NOTIFIER=false

# Set the package manager to npm
engine-strict=true

# Disable package-lock.json in workspaces
package-lock=false

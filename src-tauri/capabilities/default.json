{
  "identifier": "default-capabilities",
  "description": "Default capabilities for the Tauri application, including shell access for the backend sidecar.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/billing-backend", "sidecar": true }
      ]
    }
  ]
}
